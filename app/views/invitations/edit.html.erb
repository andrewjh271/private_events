<h4><%= @event.name %></h4>

<p>Update your invitation list.</p>


<%= form_with(url: event_invitations_path(@event), method: :patch, local: true) do |f| %>
  <%= hidden_field_tag(:event_id, @event.id) %>

  <% User.all.each do |user| %>
    <div class='checkbox'>
      <%= f.check_box("recipient[#{user.id}]", { checked: @event.invitations.exists?(recipient_id: user.id) }) %>
      <%= user.username %>
      <%#= f.label(user.id, user.username) %>
    </div>
  <% end %>
  <br>
  <%= submit_tag('Update Invitations!') %>
<% end %>

<br>
<%= button_to 'Back', @event, method: :get %>